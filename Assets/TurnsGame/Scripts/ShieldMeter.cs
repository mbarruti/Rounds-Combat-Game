using UnityEngine;
using System.Collections.Generic;

public class ShieldMeter : MonoBehaviour
{
    Stack<float> meterValues;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

}
